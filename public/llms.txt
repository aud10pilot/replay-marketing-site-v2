# Replay

> Replay makes software visible. The core product is Replay QA — an autonomous QA agent that takes a URL, explores a web application, records every session, finds real bugs, and returns a detailed bug report with root cause, suggested fix, and a full Replay recording. No test suite required. No QA team required.

## Product

- [Replay QA](https://qa.replay.io): Autonomous QA for web applications. Give it a URL and it explores the app like a user, writes Playwright tests, records every session with Replay's deterministic recording engine, and files detailed bug reports. Each report includes a root cause diagnosis, a specific suggested fix, and a full Replay recording with runtime evidence. Designed for AI-built apps and teams shipping fast.
- [Replay for CI](https://replay.io/ci-agent): Replay QA integrated into CI pipelines. Records every Playwright or Cypress test run, analyzes every failure, and posts root cause and fix to the PR automatically.
- [Replay for Debugging](https://replay.io/debugging): Record a specific bug and investigate it directly. Coding agents access the recording through Replay MCP; developers can use Replay DevTools for time-travel debugging with retroactive logpoints. Recording is available via the Replay CLI (`npx replayio record <url>`) or the Replay Chrome extension.
- [Replay QA API](mailto:sales@replay.io): REST API for embedding Replay QA as a quality gate in software factories and AI coding platforms. Every app generated by the system gets automatically tested before it ships.
- [Replay Precog](https://replay.io/precog): Deterministic analysis of potential execution state before the code is written.

## How It Works

1. **Give it a URL** — point Replay QA at any web app. No configuration, no test files. Authenticated apps are supported by providing login credentials.
2. **Autonomous exploration** — Replay QA navigates the app the way a user would, discovers meaningful user journeys, and writes Playwright tests for each one.
3. **Replay recording** — every test run is executed in a Replay-instrumented browser. The full runtime is captured deterministically: every function call, DOM mutation, network request, and state change.
4. **Bug report** — for every bug found, Replay QA files a report with root cause, a suggested fix, confidence score, and a link to the Replay recording. The report is designed to be consumed directly by a coding agent.

## Replay QA Compared to Playwright, Selenium, and Cypress

Replay QA is not a test runner. It uses Playwright internally to run the sessions it generates, and it sits a layer above whatever test suite a team already has.

| | Playwright / Selenium / Cypress | Replay QA |
| --- | --- | --- |
| Setup | Write and maintain test scripts | Give it a URL |
| Test authoring | Manual, ongoing | None; it generates its own sessions |
| Coverage | Only what someone wrote a test for | Full app surface, continuously |
| When a test fails | Stack trace and screenshot | Root cause analysis and suggested fix |
| Debugging | Manual reproduction | Time-travel through a full recording |
| Maintenance | Tests break as the UI changes | Adapts automatically, no test files |

Teams keep their existing suites. Replay QA runs in parallel and covers what those suites do not: new features shipped without tests, edge cases, full user journeys, layout shifts, slow network calls, performance regressions, accessibility failures (WCAG contrast, missing ARIA labels, keyboard traps), and runtime errors that only surface under specific timing or state conditions.

Full comparison: https://replay.io/replay-vs-test-suites

## Agent Integration

Replay QA exposes a REST API at `https://loop-qa.replay.io/api/v1`. The OpenAPI spec is at `/api/v1/openapi.json`. Agents can use this prompt to set up a continuous QA loop:

> Use Replay QA to test this app. Create a QA project for my running app, wait for the results, fix any open bugs, and keep looping until the app passes.

Full agent prompt: create a QA project with `target_url`, poll status until complete, read each open bug's root-caused report, apply the fix, mark it fixed via the API, and loop until no open bugs remain. Supports localhost apps via reverse proxy.

Replay MCP is a separate integration for debugging individual recordings. It works with Claude Code, Codex, Cursor, Windsurf, and any MCP-compatible tool, and lets an agent set logpoints, trace function calls, inspect state, and follow render chains inside a recording. Docs: https://docs.replay.io/basics/replay-mcp/overview

## Issue Tracker Integration

Replay QA supports a Bug Report Webhook. Configure an endpoint URL and Replay QA will POST each bug report as JSON with `body` (bug description), `referrer`, and a `callback_url`. POST to the `callback_url` to mark the bug as fixed and re-enable the associated journey for future runs. Compatible with Linear, GitHub Issues, Jira, and any tracker that accepts HTTP webhooks.

## Who It's For

- **Developers building with AI** — Cursor, Claude Code, Codex, Lovable, Bolt, and similar tools. AI writes the code fast; Replay QA closes the QA gap.
- **Solo builders and small teams** — no QA headcount, no test suite. Replay QA is the only QA layer they need.
- **Software factories and AI coding platforms** — embed Replay QA as a quality gate via API so every generated app is tested before it ships.
- **Teams with existing CI** — already running Playwright or Cypress? Replay QA records every run and analyzes every failure.

## The Technology

Replay captures the full browser runtime at the Chromium level — not a video or a JavaScript shim. The recording is deterministic: the same session can be replayed and inspected retroactively, with the ability to add console logs and inspect state at any point in time without re-running the app. This makes AI root cause analysis accurate in ways that aren't possible from error logs or stack traces alone.

What Replay records per session:
- Render chains (which component re-rendered, what triggered it, what changed)
- State flow (Redux actions, React state updates, context changes)
- Network timing (every request and response with exact payload and timing)
- JS execution (every function call on every frame, with arguments and return values)
- DOM mutations (what changed in the DOM, when, and what code caused it)

Replay works with any JavaScript that runs in the browser: React, Vue, Angular, Svelte, Next.js, Remix, jQuery, and vanilla JS. React apps get additional insight through built-in React DevTools, component render chain analysis, and Redux / Zustand / TanStack Query state tracking.

## Pricing

- **Free**: 20 analyses per month. No credit card required.
- **Individual**: $42/month billed annually, $49/month billed monthly. 50 analyses per month.
- **Team**: $299/month billed annually, $349/month billed monthly. 300 analyses per month.
- **Enterprise**: Custom, usage-based or seat-based. Includes API access for software factory integration.

ROI calculator: https://replay.io/roi-calculator

## Pages

- [Home](https://replay.io/): Replay QA overview and product entry point.
- [How It Works](https://replay.io/how-it-works): Step-by-step walkthrough from URL to bug report.
- [Replay QA vs. Test Suites](https://replay.io/replay-vs-test-suites): How Replay QA compares to Playwright, Selenium, and Cypress.
- [Replay QA](https://replay.io/qa): Autonomous QA for the vibecoding era.
- [Replay for CI](https://replay.io/ci-agent): Automated analysis on every CI test failure.
- [Fix Flaky Tests](https://replay.io/fix-flaky-tests): Root cause and suggested fix posted as a PR comment.
- [Replay for Debugging](https://replay.io/debugging): Record a bug, investigate with Replay MCP or DevTools.
- [For Engineers](https://replay.io/for-engineers): AI-assisted CI/CD for engineering teams.
- [For Vibe Coders](https://replay.io/for-vibe-coders): Debugging for apps built with Lovable, Bolt, and similar tools.
- [Replay Precog](https://replay.io/precog): Deterministic debugging for code that doesn't exist yet.
- [Pricing](https://replay.io/pricing): Plans, limits, and billing.
- [ROI Calculator](https://replay.io/roi-calculator): Estimate debugging time and cost saved.
- [Design Partner](https://replay.io/design-partner): Free Replay MCP access and direct engineering time.
- [About](https://replay.io/about): Team, history, and mission.
- [Security](https://replay.io/security): Encryption, compliance, and privacy practices.
- [Privacy](https://replay.io/privacy) and [Terms](https://replay.io/terms)
- [Blog: Introducing Replay Precog](https://replay.io/blog/introducing-replay-precog)
- Docs: https://docs.replay.io
- Discord: https://replay.io/discord

## Company

Founded by ex-Mozilla engineers who spent a decade building the Firefox browser engine. Replay's recording technology is used by engineering teams at Vercel, Glide, Pantheon, Metabase, Tablecheck, and more. Endorsed by React maintainers Dan Abramov and Sebastian Markbåge.

Contact: support@replay.io for support, sales@replay.io for enterprise and API access.

Mission: Make the invisible visible — so every app ships with a QA layer, whether it's built by a solo developer, an AI agent, or a software factory.
